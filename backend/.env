# Copy this file to ".env" and fill in real values.

ENV=development

# Paste the ngrok URL printed by the Kaggle notebook here (updates each run).
KAGGLE_AI_BASE_URL=https://shudder-reviver-undercoat.ngrok-free.dev

# Seconds to wait for long-running AI operations (RAG build, generation, etc.)
KAGGLE_REQUEST_TIMEOUT=600
